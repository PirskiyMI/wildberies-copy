import { FC } from 'react';
import { FavoriteButton } from '../../../shared/ui';

export const ToggleToFavorite: FC = () => {
   const clickHandler = () => {};
   return <FavoriteButton clickHandler={clickHandler} />;
};
