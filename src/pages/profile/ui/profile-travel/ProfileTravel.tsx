import { FC } from 'react';
import styles from './ProfileTravel.module.scss';

const ProfileTravel: FC = () => {
   return <div className={styles.travel}>ProfileTravel</div>;
};

export default ProfileTravel;
