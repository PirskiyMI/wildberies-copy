import { FC } from 'react';
import styles from './ProfileDelivery.module.scss';

const ProfileDelivery: FC = () => {
   return <div className={styles.delivery}>ProfileDelivery</div>;
};

export default ProfileDelivery;
