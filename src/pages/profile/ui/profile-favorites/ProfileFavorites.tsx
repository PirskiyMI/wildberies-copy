import { FC } from 'react';
import styles from './ProfileFavorites.module.scss';

const ProfileFavorites: FC = () => {
   return <div className={styles.favorites}>ProfileFavorites</div>;
};

export default ProfileFavorites;
