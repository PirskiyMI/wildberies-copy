import { FC } from 'react';
import styles from './ProfileAppeals.module.scss';

const ProfileAppeals: FC = () => {
   return <div className={styles.appels}>ProfileAppeals</div>;
};

export default ProfileAppeals